// Re-export commonly used Prisma model types for type-safe usage across the app.
// Import from the generated Prisma client to avoid `any` annotations.

export type {
  AdminUser,
  SiteSettings,
  SeoSettings,
  NavigationItem,
  HeroSection,
  AboutSection,
  ServiceItem,
  Game,
  GameImage,
  ContactInfo,
  ContactSubmission,
  SocialLink,
  FooterSettings,
  PrivacyPolicy,
  TermsOfService,
} from '@/generated/prisma/client'
